export interface IPerson {
  name: string;
  id: string;
  img: string;
}

export interface IPersonInfo {
  title: string;
  data: string;
}

export type PeopleListItem = {
  id: string;
  img: string;
  name: string;
}
